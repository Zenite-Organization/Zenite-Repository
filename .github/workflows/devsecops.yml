name: DevSecOps Pipeline (Python)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write
  security-events: write

jobs:
  ##########################################
  # 1. SOURCE - Análise Estática (SAST)
  ##########################################
  source:
    name: 🔍 Source Analysis (Semgrep & Gitleaks)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Semgrep (SAST)
        uses: returntocorp/semgrep-action@v1
        with:
          config: "p/owasp-top-ten"

      # - name: Detect Secrets (Gitleaks)
      #   uses: gitleaks/gitleaks-action@v2
      #   with:
      #     args: detect --source . --verbose

  ##########################################
  # 2. BUILD - Instalação e Escaneamento
  ##########################################
  build:
    name: 🧱 Build & Dependency Scans
    runs-on: ubuntu-latest
    needs: source

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Trivy on Dependencies
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          ignore-unfixed: true
          severity: 'HIGH,CRITICAL'

      - name: Build Docker image
        run: docker build -t myapp:latest .

      - name: Run Trivy on Docker image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'myapp:latest'
          format: 'table'
          exit-code: '0'
          severity: 'HIGH,CRITICAL'

      - name: Save Docker image
        run: docker save myapp:latest -o myapp.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: myapp-image
          path: myapp.tar


  ##########################################
  # 3. TEST - Dynamic Security Tests (DAST)
  ##########################################

  test:
    name: 🧪 Security Testing (OWASP ZAP)
    runs-on: ubuntu-latest
    needs: build

    env:
      WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: myapp-image
          path: .

      - name: Load Docker image
        run: docker load -i myapp.tar

      - name: Start container with environment variables
        run: |
          echo "🚀 Iniciando container da aplicação..."
          docker run -d -p 8000:8000 --name myapp \
            -e WEBHOOK_SECRET="${{ secrets.WEBHOOK_SECRET }}" \
            -e GEMINI_API_KEY="${{ secrets.GEMINI_API_KEY }}" \
            myapp:latest

      - name: Wait for app to be ready
        run: |
          echo "⏳ Aguardando aplicação iniciar..."
          for i in {1..10}; do
            if curl -s http://localhost:8000 > /dev/null; then
              echo "✅ Aplicação está respondendo!"
              exit 0
            fi
            echo "Tentativa $i de 10: app ainda não respondeu"
            sleep 5
          done
          echo "❌ Aplicação não iniciou a tempo"
          docker logs myapp
          exit 1

      - name: Run OWASP ZAP baseline scan
        uses: zaproxy/action-baseline@v0.9.0
        with:
          target: 'http://localhost:8000'
          cmd_options: '-J report_json.json -w report_md.md -r report_html.html'
        continue-on-error: true

      - name: Stop container
        if: always()
        run: |
          echo "🧹 Encerrando container..."
          docker stop myapp || true
          docker rm myapp || true


  ##########################################
  # 4. RELEASE - Aprovação e Validação
  ##########################################
  release:
    name: 🚀 Release Validation
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://myapp.com
    steps:
      - name: Verify release
        run: echo "✅ All checks passed — release approved."

  ##########################################
  # 5. DEPLOY - Infraestrutura e Compliance
  ##########################################
  deploy:
    name: ☁️ IaC & Compliance Scan
    runs-on: ubuntu-latest
    needs: release
    steps:
      - uses: actions/checkout@v4

      - name: Run Checkov (IaC Scan)
        uses: bridgecrewio/checkov-action@master
        with:
          directory: ./infra
          soft_fail: true

  ##########################################
  # 6. DEPLOY TO ORACLE VM
  ##########################################
  deploy-prod:
    name: 🚀 Deploy to Oracle VM
    runs-on: ubuntu-latest
    needs: release
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Download image artifact
        uses: actions/download-artifact@v4
        with:
          name: myapp-image
          path: .

      - name: Copy image to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_SSH_USER }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_SSH_PORT || '22' }}
          source: "myapp.tar"
          target: "/home/${{ secrets.ORACLE_SSH_USER }}/"

      - name: SSH into VM and deploy image
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_SSH_USER }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_SSH_PORT || '22' }}
          script: |
            sudo docker load -i /home/${{ secrets.ORACLE_SSH_USER }}/myapp.tar
            sudo docker stop myapp || true
            sudo docker rm myapp || true
            sudo docker run -d --name myapp -p 8000:8000 \
              -e WEBHOOK_SECRET=${{ secrets.WEBHOOK_SECRET }} \
              -e GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }} \
              -e APP_ID=${{ secrets.APP_ID }} \
              -e APP_PRIVATE_KEY="${{ secrets.APP_PRIVATE_KEY }}" \
              myapp:latest
